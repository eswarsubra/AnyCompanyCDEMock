# Runtime dependencies:
# - boto3: Amazon Translate client used by the translation stage.
# - anthropic: AnthropicBedrock client for summarization + quality scoring
#   (see ADR-0002); constructed lazily at runtime, never at import.
# - jsonschema: per-record review validation in the ingestion stage
#   (review_pipeline/ingestion.py). Pulls in attrs / referencing / rpds-py.
#   (config + logging remain dependency-free standard library.)
boto3
anthropic
jsonschema
# urllib3 is a transitive dependency (via botocore / anthropic's httpx). Pin it
# clear of the known cookie-leak / cross-origin redirect / decompression-DoS
# advisories (dependency scanning: ProtoShield/grype). The Lambda bundle pins the
# same floor in lambda-requirements.txt; this keeps the source manifest aligned.
urllib3>=2.7.0
# Test/dev dependencies:
pytest
